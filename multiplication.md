# Multiplication of Integers

Multiplication means repeated addition (for positive integers).
The sign rule is:

* positive × positive = positive
* negative × positive = negative
* negative × negative = positive

**Example:**
(-3) × 4 = -12
